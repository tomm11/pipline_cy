// Shows commands
///<reference types="Cypress"/> 
require('cypress-plugin-tab')

    /*describe ("Default" , () =>{
        it("Test" , () =>{
            cy.visit()
            cy.title()

        })
    })*/


    //Comandos de consola propios de cypress

    //npx cypress run / corre todos los test 
    //npx cypress run --headed / va a correr todos los script pero desde el navegador
    //npx cypress run --browser chrome
    //npx cypress run --spec "cypress" la otra / y buscar el archivo. 
    //npx cypress run --spec y al final * para que ejecute todo lo de una carpeta